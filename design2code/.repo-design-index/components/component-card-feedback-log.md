# 组件卡片人工反馈日志

## 2026-05-27 TableBase

反馈内容：

```txt
TableBase组件已经不推荐使用，统一使用新表格组件baseTableV2
```

处理结果：

- 已更新 `.repo-design-index/components/cards/TableBase.md`。
- 将 `说明状态` 改为 `human-reviewed`。
- 增加 `推荐状态`：`deprecated-for-new-pages`。
- 增加 `替代组件`：`baseTableV2`。
- 在适用场景、不适用场景和生成页面注意事项中明确：新页面不要使用 `TableBase`，表格类页面统一使用 `baseTableV2`。
